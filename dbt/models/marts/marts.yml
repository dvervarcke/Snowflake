version: 2

models:
  - name: fct_taxi_rides
    description: Fact-like model with trip duration derived in minutes.
    columns:
      - name: trip_minutes
        tests:
          - not_null
          - dbt_utils.accepted_range:
              min_value: 0
              inclusive: true

